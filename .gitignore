node_modules/
.vite/
coverage/
# dist/ is intentionally committed (static build for direct hosting; see DEPLOY.md)

# Playwright
/test-results/
/playwright-report/
/blob-report/
/.playwright/

# Oracle build artifacts (generated from Orig/ on demand; outputs are committed)
/tools/oracle/build/
/tools/oracle/tmModelTester

# Wasm build intermediates (patched source copy). The generated ES module under
# src/wasm/generated/ IS committed so the app/tests run without Emscripten.
/tools/wasm/build/

# Editor / OS
.DS_Store
*.log
